export const PROJECT_PREFIX = 'UniSearch';

export const SESSION_STORAGE_KEYS = {
  CacheClearedOn: `${PROJECT_PREFIX}_CacheClearedOn`,
  BubbleId: `${PROJECT_PREFIX}_BubbleId`,
  Token: `${PROJECT_PREFIX}_Token`,
};
